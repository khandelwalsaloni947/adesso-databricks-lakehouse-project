# UDFs and Business Rules

Include at least one meaningful UDF.

Good examples:
- classify market priority
- assign a custom business segment
- normalize custom warehouse status

Use a UDF only where built-in Spark functions are not enough.
