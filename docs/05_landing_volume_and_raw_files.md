# Landing Volume and Raw Files

The raw files in this project should live in the governed `landing` volume.

Suggested folder split inside the landing volume:
- `sales/`
- `inventory/`
- `reference/`

The raw files in `sample_data/` should be copied into those governed locations before ingestion.
